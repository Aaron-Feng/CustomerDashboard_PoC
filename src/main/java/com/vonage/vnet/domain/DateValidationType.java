package com.vonage.vnet.domain;

/**
 * DateValidationType enumeration class holds the validation types  (Past Date, Future Date, Ongoing(current and/or future)) for date.
 *
 */
public enum DateValidationType {
    PAST_DATE, FUTURE_DATE, ONGOING;
}
